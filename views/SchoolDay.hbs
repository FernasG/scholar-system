<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>SED - Dia Letivo</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../css/main.css">
  <link rel="stylesheet" href="../css/school-days.css">

  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.6.8/axios.min.js"
    integrity="sha512-PJa3oQSLWRB7wHZ7GQ/g+qyv6r4mbuhmiDb8BjSFZ8NZ2a42oTtAq5n0ucWAwcQDlikAtkub+tPVCw4np27WCg=="
    crossorigin="anonymous" referrerpolicy="no-referrer"></script>

  <script src="../js/main.js"></script>
  <script src="../js/school-days.js"></script>
</head>

<body>
  <header class="navbar navbar-dark">
    <a class="navbar-brand" href="#">
      SED
    </a>
    <span class="navbar-text">
      Bem-vindo, Prof. <strong></strong>
    </span>
    <ul class="nav">
      <li class="nav-item"><a class="nav-link" href="../students">Alunos</a></li>
      <li class="nav-item"><a class="nav-link" href="../classes">Turmas</a></li>
      <li class="nav-item"><a class="nav-link" href="#">Meu Perfil</a></li>
      <li class="nav-item"><a class="nav-link" href="/">Sair</a></li>
    </ul>
  </header>

  <main class="container mt-4">
    <div class="row">
      <div class="col">
        <p>
          <span>Dia Letivo: </span>
          <span>{{school_day.date}}</span>
        </p>
      </div>
    </div>

    <div class="row">
      <div class="col-md-8">
        <div class="card mb-4">
          <div class="card-header">
            Anotação de Aula
          </div>
          <div class="card-body">
            {{#if school_day.annotations}}
            <form>
              <div class="form-group mb-3">
                <textarea class="form-control" id="classAnnotations" rows="10">{{school_day.annotations.text}}</textarea>
              </div>

              <div class="form-group d-flex justify-content-center">
                <button class="btn btn-primary mb-2 w-50" type="button" onclick="updateAnnotation('{{school_day.annotations.id}}')">
                  Salvar
                </button>
              </div>
            </form>
            {{else}}
            <div class="d-flex justify-content-center">
              <button class="btn btn-primary mb-2 w-50" onclick="createAnnotation('{{school_day.id}}')">
                Criar Anotações de Aula
              </button>
            </div>
            {{/if}}
          </div>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-header">
            Lista de Presença
          </div>
          <div class="card-body">
            {{#if school_day.attendance_lists}}
            <ul class="list-group">
              {{#each school_day.attendance_lists}}
              <li class="list-group-item">
                <p class="mb-0">{{this.student_name}}</p>
                {{#if this.attendance}}
                <input class="form-check-input me-1" type="checkbox" onclick="updateAttendanceList(this, '{{this.id}}')"
                  checked value="{{this.id}}">
                {{else}}
                <input class="form-check-input me-1" type="checkbox" onclick="updateAttendanceList(this, '{{this.id}}')"
                  value="{{this.id}}">
                {{/if}}
              </li>
              {{/each}}
            </ul>
            {{else}}
            <button class="btn btn-primary mb-2 w-100" onclick="createAttendanceList('{{school_day.id}}')">
              Criar Lista de Presença
            </button>
            {{/if}}
          </div>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="../js/global.js"></script>

</body>

</html>