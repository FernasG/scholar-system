<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>SED - Turmas</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="../css/main.css">
  <link rel="stylesheet" href="../css/classes.css">

  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.6.8/axios.min.js"
    integrity="sha512-PJa3oQSLWRB7wHZ7GQ/g+qyv6r4mbuhmiDb8BjSFZ8NZ2a42oTtAq5n0ucWAwcQDlikAtkub+tPVCw4np27WCg=="
    crossorigin="anonymous" referrerpolicy="no-referrer"></script>

  <script src="../js/main.js"></script>
</head>

<body>
  <header class="navbar navbar-dark">
    <a class="navbar-brand" href="#">
      SED
    </a>
    <span class="navbar-text">
      Bem-vindo, Prof. <strong></strong>
    </span>
    <ul class="nav">
      <li class="nav-item"><a class="nav-link" href="../students">Alunos</a></li>
      <li class="nav-item"><a class="nav-link" href="../classes">Turmas</a></li>
      <li class="nav-item"><a class="nav-link" href="#">Meu Perfil</a></li>
      <li class="nav-item"><a class="nav-link" href="/">Sair</a></li>
    </ul>
  </header>

  <main class="container mt-4">
    <div class="modal fade" id="updateClassStudents" tabindex="-1" aria-labelledby="updateClassStudentsLabel"
      aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="updateClassStudentsLabel">Adicionar Alunos</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
            {{#if students}}
            <ul class="list-group">
              {{#each students}}
              <li class="list-group-item">
                {{#if this.checked}}
                <input class="form-check-input me-1" type="checkbox" checked value="{{this.id}}">
                {{else}}
                <input class="form-check-input me-1" type="checkbox" value="{{this.id}}">
                {{/if}}
                {{this.name}}
              </li>
              {{/each}}
            </ul>
            {{else}}
            <p class="mb-0">Não há nenhum aluno registrado no sistema.</p>
            {{/if}}
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-primary" onclick="saveClassStudents('{{class.id}}')">Salvar
              Alterações</button>
            <button type="button" class="btn btn-warning" data-bs-dismiss="modal">Fechar</button>
          </div>
        </div>
      </div>
    </div>

    <div class="modal fade" id="addSchoolDay" tabindex="-1" aria-labelledby="addSchoolDayLabel" aria-hidden="true">
      <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title" id="addSchoolDayLabel">Adicionar Dia Letivo</h5>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
            <div class="mb-3">
              <label for="classDate" class="form-label">Data: </label>
              <input type="date" class="form-control" id="classDate">
            </div>
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-primary" onclick="createSchoolDay('{{class.id}}')">Salvar</button>
            <button type="button" class="btn btn-warning" data-bs-dismiss="modal">Fechar</button>
          </div>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col">
        <p class="spaced">
          <span>{{ class.name }} - {{ class.description }}</span>
          <span>
            <button class="btn btn-danger" onclick="removeClass('{{class.id}}')">
              Remover Turma
            </button>
          </span>
        </p>
      </div>
    </div>

    <div class="row">
      <div class="col-md-8">
        <div class="card mb-4">
          <div class="card-header">
            <div>
              Alunos
            </div>
            <div>
              <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#updateClassStudents">
                Adicionar Alunos
              </button>
            </div>
          </div>
          <div class="card-body">
            {{#if class.students}}
            <ul class="list-group">
              {{#each class.students}}
              <li class="list-group-item">
                {{this.name}}
              </li>
              {{/each}}
            </ul>
            {{else}}
            <p class="mb-0">Essa turma não tem nenhum aluno matriculado.</p>
            {{/if}}
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-header">
            <div>
              Dias Letivos
            </div>
            <div>
              <button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addSchoolDay">
                Adicionar Dia Letivo
              </button>
            </div>
          </div>
          <div class="card-body">
            <ul class="list-group">
              {{#if class.school_days}}
              {{#each class.school_days}}
              <li class="list-group-item">
                <a href="../school-days/{{this.id}}">Data: {{this.date}}</a>
              </li>
              {{/each}}
              {{else}}
              <p class="mb-0">Essa turma não tem nenhum dia letivo cadastrado.</p>
              {{/if}}
            </ul>
          </div>
        </div>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="../js/global.js"></script>
  <script src="../js/classes.js"></script>

</body>

</html>